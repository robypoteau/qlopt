#include <unsupported/Eigen/MPRealSupport>
#include <string>
#include "misc.h"
	
using namespace mpfr;
using namespace Eigen;
using namespace std;

/*	
solverInput getAll(){
	solverInput input;
	input.funcName = getFuncName();
	input.time     = getTimeInterval();
	input.msmt     = getMsmt();
	uinput.Not     = getInitParam();
	input.yNot     = getInitVal();
}

string getFuncName(){
	return "lotka_volterra";
	//scanf();
}

vec getTimeInterval(mpreal a, mpreal b, int n){
	vec time(n+1);
	for(int i=a; i<=b; i++){
		time(i) = a+i*((b-a)/n)
	}
	return time;
}

vec getTimeInterval(mpreal a, mpreal b){ 
	return ;
}

vec getMsmt();
vec getInitParam();
vec getInitVal();*/